# JMeter user-defined properties для lab4.
# Скопируй в env.properties (gitignored) и подставь свои значения варианта.
# Запуск: ./gradlew :lab4:loadTestAll (или :stressTest), JMeter подхватит файл через -q.

token=518504752
user=-1355042415

# host и port совпадают для всех 3 конфигураций, переопределять обычно не нужно
host=stload.se.ifmo.ru
port=8080
