"""GOST report для лабораторной работы №4: нагрузочное и стресс-тестирование JMeter.

Запуск:
    python3 ~/.claude/skills/gost-report/scripts/ensure_env.py \\
        lab4/.claude/gost-report/build.py
"""
from pathlib import Path

from gost_report import Report, TitleConfig

LAB4 = Path(__file__).resolve().parents[2]
FIGURES = LAB4 / 'docs' / 'figures'

r = Report(TitleConfig(
    work_type="Лабораторная работа",
    work_number="№4",
    topic="Нагрузочное и стресс-тестирование веб-приложения с помощью Apache JMeter",
    variant="95033",
    student_name="Фамилия И.О.",
    student_group="P3XXX",
    teacher_name="Фамилия И.О.",
    teacher_label="Проверил",
    teacher_degree="к.т.н.",
    teacher_position="доцент",
    year="2026",
))

r.toc()

# ============================================================
r.h1("Текст задания")
# ============================================================

r.text(
    "С помощью программного пакета Apache JMeter провести нагрузочное и "
    "стресс-тестирование веб-приложения в соответствии с вариантом задания."
)
r.text(
    "В ходе нагрузочного тестирования необходимо протестировать 3 конфигурации "
    "аппаратного обеспечения и выбрать среди них наиболее дешёвую, удовлетворяющую "
    "требованиям по максимальному времени отклика приложения при заданной нагрузке."
)
r.text(
    "В ходе стресс-тестирования необходимо определить, при какой нагрузке выбранная "
    "на предыдущем шаге конфигурация перестаёт удовлетворять требованиям по "
    "максимальному времени отклика. Для этого нужно построить график зависимости "
    "времени отклика приложения от нагрузки."
)

r.h2("Параметры варианта")

r.text("Приложение доступно только во внутренней сети кафедры. Тестовый сервер размещён по адресу stload.se.ifmo.ru:8080. Если запрос содержит некорректные параметры, сервер возвращает HTTP 403. Если приложение не справляется с нагрузкой, сервер возвращает HTTP 503.")

r.table(
    [
        ["Параметр", "Значение"],
        ["URL config=1 ($4500)", "http://stload.se.ifmo.ru:8080?token=518504752&user=-1355042415&config=1"],
        ["URL config=2 ($7300)", "http://stload.se.ifmo.ru:8080?token=518504752&user=-1355042415&config=2"],
        ["URL config=3 ($9300)", "http://stload.se.ifmo.ru:8080?token=518504752&user=-1355042415&config=3"],
        ["Максимальное число параллельных пользователей", "9"],
        ["Средняя нагрузка одного пользователя", "20 запр./мин"],
        ["Максимально допустимое время отклика (SLO)", "720 мс"],
        ["Код ответа при некорректных параметрах", "HTTP 403"],
        ["Код ответа при перегрузке", "HTTP 503"],
    ],
    "Параметры тестируемого веб-приложения",
)

r.text("Целевая суммарная нагрузка на сервер по варианту получается умножением числа параллельных пользователей на индивидуальный рейт запросов:")
f_load = r.formula(r"\lambda_{\text{target}} = N_{\text{users}} \cdot R_{\text{user}} = 9 \cdot 20 = 180 \text{ req/min} = 3 \text{ req/s}")
r.text(f"Формула ({f_load}) задаёт нагрузку, которую инструмент должен реально воспроизвести на сервере, иначе результаты сравнения конфигураций не будут соответствовать варианту.")

# ============================================================
r.h1("Описание конфигурации JMeter для нагрузочного тестирования")
# ============================================================

r.text(
    "План построен в формате .jmx и запускается через CLI-режим JMeter "
    "(jmeter -n), что соответствует рекомендации Apache JMeter не использовать "
    "GUI для нагрузочных прогонов. Параметры варианта (token, user, host, port, "
    "config) вынесены в файл .properties и подключаются через ключ -q, "
    "конфигурация выбирается через -Jconfig=N. Это позволяет одним планом "
    "тестировать все три URL варианта без копирования плана."
)

r.h2("Структура плана")

r.bullet([
    "User Defined Variables на уровне Test Plan: host, port, token, user, config. Значения берутся из env.properties через __P(host), __P(token), __P(user) и так далее.",
    "HTTP Request Defaults: domain ${host}, port ${port}, protocol http, connect_timeout 5000 мс, response_timeout 30000 мс.",
    "HTTP Cookie Manager и HTTP Cache Manager с очисткой на каждой итерации, чтобы сэмплы были независимыми и не кэшировались.",
    "Один Thread Group с 9 потоками (по числу пользователей варианта), включён scheduler.",
    "Constant Throughput Timer внутри Thread Group: calcMode = 0 (this thread only), throughput = 20.0 запросов в минуту на поток. При 9 активных потоках суммарный таргет 180 запросов в минуту, что совпадает с целевой нагрузкой варианта.",
    "HTTP Request Sampler: GET /, параметры token, user, config передаются как query-параметры.",
    "Response Assertion: код ответа должен быть 200. Реальные HTTP-ошибки (403 при кривых параметрах, 503 при перегрузке) будут зафиксированы в .jtl.",
])

r.text("Параметры Thread Group для нагрузочного тестирования:")
r.table(
    [
        ["Параметр", "Значение"],
        ["Number of Threads (users)", "9"],
        ["Ramp-Up Period (sec)", "10"],
        ["Scheduler enabled", "true"],
        ["Duration (sec)", "60"],
        ["Loop Count", "infinite (управляется планировщиком)"],
        ["On Sample Error", "continue"],
        ["Same user on next iteration", "false"],
    ],
    "Параметры Thread Group в нагрузочном плане",
)

r.text("Параметры Constant Throughput Timer:")
r.table(
    [
        ["Параметр", "Значение"],
        ["Target throughput, samples per minute", "20.0"],
        ["Calculate Throughput based on", "this thread only (calcMode=0)"],
    ],
    "Параметры Constant Throughput Timer в нагрузочном плане",
)

r.text("Запуск нагрузочного прогона по одной конфигурации в CLI-режиме:")
r.code(
    "jmeter -n \\\n"
    "  -t lab4/jmeter/plans/load-test.jmx \\\n"
    "  -q lab4/jmeter/env.properties \\\n"
    "  -Jconfig=1 \\\n"
    "  -l lab4/jmeter/results/load/config1.jtl \\\n"
    "  -e -o lab4/jmeter/reports/load-config1"
)
r.text("Аналогично с -Jconfig=2 и -Jconfig=3. Длительность одного прогона около 70 секунд, за это время собирается порядка 170 сэмплов на конфигурацию.")

# ============================================================
r.h1("Результаты нагрузочного тестирования")
# ============================================================

r.text("Сводная статистика по трём конфигурациям при целевой нагрузке 9 пользователей по 20 запр./мин (суммарно 180 запр./мин):")

r.table(
    [
        ["Config", "Cost", "Samples", "Avg, мс", "p90, мс", "p95, мс", "p99, мс", "Throughput, req/s", "Verdict"],
        ["1", "$4500", "171", "1074", "1153", "1192", "1324", "2.80", "FAIL"],
        ["2", "$7300", "171", "832", "880", "894", "946", "2.80", "FAIL"],
        ["3", "$9300", "171", "450", "489", "499", "530", "2.85", "PASS"],
    ],
    "Сводные результаты нагрузочного тестирования",
)

r.text("Все три конфигурации обработали запросы с HTTP 200, ошибок 4xx и 5xx не зафиксировано. Это означает, что отказа в обслуживании при заданной нагрузке нет, но время отклика существенно отличается между конфигурациями.")

r.figure("load_throughput.png", "Пропускная способность приложения по конфигурациям при нагрузке 180 req/min")

r.text("Все три конфигурации обработали 2.80-2.85 req/s, что близко к целевым 3.0 req/s. Это значит, что сервер успевает принимать поток запросов на всех конфигурациях, разница между ними проявляется только во времени отклика.")

r.figure("load_response_time.png", "Среднее время отклика и 95-й перцентиль по конфигурациям")

r.text("Линия SLO 720 мс на графике делит конфигурации на две группы. Config 1 и Config 2 находятся выше линии и не удовлетворяют требованиям варианта. Config 3 даёт avg 450 мс и p95 499 мс, что укладывается в SLO с запасом порядка 31 процента от верхней границы.")

# ============================================================
r.h1("Выводы по выбранной конфигурации аппаратного обеспечения")
# ============================================================

r.text("Из трёх конфигураций под заданной нагрузкой 180 запр./мин в SLO 720 мс укладывается только Config 3 ($9300). Config 1 при той же нагрузке отвечает в среднем за 1074 мс (превышение SLO на 49 процентов), Config 2 за 832 мс (превышение на 16 процентов). Самой дешёвой из удовлетворяющих требованиям является Config 3, она и выбирается для дальнейшего стресс-тестирования.")

r.numbered([
    "Под целевой нагрузкой 9 пользователей x 20 запр./мин Config 1 ($4500) не проходит SLO: avg 1074 мс, p95 1192 мс.",
    "Config 2 ($7300) также не проходит SLO: avg 832 мс, p95 894 мс. Превышение умеренное, но требование варианта 720 мс не выполнено.",
    "Config 3 ($9300) проходит SLO с запасом: avg 450 мс, p95 499 мс. Доля сэмплов вне SLO нулевая.",
    "Самой дешёвой из удовлетворяющих требованиям является Config 3. Несмотря на максимальную цену, у других конфигураций требуемое время отклика просто недостижимо.",
    "Сервер на всех трёх конфигурациях обрабатывает поток без отказов: HTTP 503 ни разу не возвращён, доля HTTP 200 во всех прогонах равна 100 процентам.",
])

# ============================================================
r.h1("Описание конфигурации JMeter для стресс-тестирования")
# ============================================================

r.text(
    "Стресс-план построен ступенчатым: 9 последовательных Thread Group, каждая держит "
    "постоянное число пользователей в течение 30 секунд при той же индивидуальной "
    "нагрузке 20 запр./мин на пользователя. На уровне Test Plan включён флаг "
    "serialize_threadgroups, поэтому группы запускаются строго одна за другой, без "
    "наложения. Это даёт чистые точки на графике зависимости времени отклика от "
    "количества параллельных пользователей."
)

r.text("Параметры ступеней:")
r.table(
    [
        ["№", "Users", "Ramp-Up, сек", "Duration, сек", "Target throughput на поток, req/min"],
        ["1", "9",  "5",  "30", "20"],
        ["2", "12", "5",  "30", "20"],
        ["3", "15", "5",  "30", "20"],
        ["4", "20", "5",  "30", "20"],
        ["5", "25", "5",  "30", "20"],
        ["6", "30", "5",  "30", "20"],
        ["7", "45", "5",  "30", "20"],
        ["8", "60", "5",  "30", "20"],
        ["9", "80", "5",  "30", "20"],
    ],
    "Конфигурация ступеней стресс-тестирования",
)

r.text("Шаг по числу пользователей сгущён в области ожидаемого перелома (от 20 до 45 пользователей) и разрежен на хвостах. Общая длительность прогона около 4.5 минут (9 ступеней по 30 секунд). На каждой ступени собирается от 80 до 600 сэмплов в зависимости от числа активных пользователей.")

r.text("Запуск стресс-прогона в CLI-режиме:")
r.code(
    "jmeter -n \\\n"
    "  -t lab4/jmeter/plans/stress-test.jmx \\\n"
    "  -q lab4/jmeter/env.properties \\\n"
    "  -Jconfig=3 \\\n"
    "  -l lab4/jmeter/results/stress/stress-config3.jtl \\\n"
    "  -e -o lab4/jmeter/reports/stress"
)

# ============================================================
r.h1("Результаты стресс-тестирования")
# ============================================================

r.text("Стресс-тест проводился на выбранной конфигурации Config 3 ($9300). В таблице ниже значения по каждой стационарной ступени (отфильтрованы переходные сэмплы во время ramp-up следующей группы):")

r.table(
    [
        ["Users", "Samples", "Avg, мс", "p95, мс", "Доля > SLO", "Status"],
        ["9",  "84",  "473", "602", "1.2%",  "PASS"],
        ["12", "107", "465", "524", "0.9%",  "PASS"],
        ["15", "126", "474", "505", "1.6%",  "PASS"],
        ["20", "165", "497", "556", "0.6%",  "PASS"],
        ["25", "195", "489", "537", "1.0%",  "PASS"],
        ["30", "231", "533", "586", "0.4%",  "PASS"],
        ["45", "344", "598", "662", "0.6%",  "PASS (граница)"],
        ["60", "453", "682", "763", "28.7%", "FAIL (точка перелома)"],
        ["80", "598", "857", "959", "84.3%", "FAIL"],
    ],
    "Время отклика по ступеням стресс-тестирования (Config 3)",
)

r.figure("stress_response_vs_load.png", "Зависимость времени отклика приложения от количества параллельных пользователей")

r.text("На графике видно линейный рост avg и p95 от 9 до 60 пользователей, причём p95 пересекает линию SLO 720 мс при N=60. При 60 пользователях p95 уже 763 мс, и доля сэмплов выше SLO достигает 28.7 процентов. На ступени 80 пользователей avg выходит за SLO (857 мс), а 84 процента запросов превышают порог.")

r.text("HTTP-ошибки в стресс-прогоне не возвращены ни на одной ступени, сервер сохраняет 100 процентов HTTP 200 вплоть до 80 параллельных пользователей. Это означает, что отказ происходит не по доступности (нет 503), а по времени отклика, что и было целью стресс-измерения.")

r.text("Соотношение точки перелома к номинальной нагрузке варианта:")
f_break = r.formula(r"k_{\text{stress}} = \frac{N_{\text{break}}}{N_{\text{users}}} = \frac{60}{9} \approx 6.7")
r.text(f"Согласно формуле ({f_break}), Config 3 выдерживает примерно 6.7-кратное превышение номинальной пользовательской нагрузки до выхода p95 за SLO. Это запас по числу одновременных пользователей при сохранении того же поведения каждого пользователя (20 запр./мин).")

# ============================================================
r.h1("Выводы по работе")
# ============================================================

r.numbered([
    "В Apache JMeter построены два плана: нагрузочный (один Thread Group, 9 пользователей, Constant Throughput Timer 20 req/min на поток) и стресс-план (9 последовательных Thread Group со ступенчатым ростом пользователей от 9 до 80).",
    "Корректность воспроизведения целевой нагрузки 180 req/min подтверждена замером пропускной способности: 2.80-2.85 req/s по всем трём конфигурациям, что согласуется с расчётным значением 3.0 req/s (погрешность связана с временем отклика сервера и режимом таймера).",
    "Нагрузочное тестирование трёх конфигураций показало, что SLO 720 мс выдерживает только Config 3 ($9300): avg 450 мс, p95 499 мс. Config 1 ($4500) даёт avg 1074 мс, Config 2 ($7300) даёт avg 832 мс, обе не удовлетворяют требованию.",
    "Самая дешёвая из удовлетворяющих требованиям конфигураций Config 3 ($9300). Несмотря на то, что она же самая дорогая в варианте, среди представленных трёх альтернатив других подходящих нет.",
    "Стресс-тестирование Config 3 со ступенчатым ростом числа параллельных пользователей от 9 до 80 показало плавный рост времени отклика. При 9 пользователях avg 473 мс, при 45 пользователях 598 мс, при 60 пользователях 682 мс.",
    "Точка перелома по 95-му перцентилю достигается при 60 параллельных пользователях: p95 = 763 мс, что выше SLO. По avg точка перелома сдвинута к 80 пользователям (avg = 857 мс).",
    "Запас по нагрузке Config 3 относительно требований варианта составляет примерно 6.7 раза по числу пользователей при сохранении индивидуальной нагрузки 20 запр./мин.",
    "HTTP-отказов (503) не зафиксировано ни в нагрузочном, ни в стресс-режиме. Деградация качества обслуживания происходит исключительно через рост времени отклика, поэтому именно SLO 720 мс является ограничивающим фактором, а не пропускная способность как таковая.",
])

out = r.save(LAB4 / "docs" / "report.docx")
print(f"Wrote {out}")
